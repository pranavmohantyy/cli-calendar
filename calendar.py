import calendar\nimport datetime\nfrom dataclasses import dataclass\n\n@dataclass\nclass Event:\n    date: str\n    time: str\n    title: str\n    description: str\n    reminder_minutes: int\n\nevents = []\n\ndef render_month():\n    now = datetime.datetime.now()\n    month = now.month\n    year = now.year\n    month_calendar = calendar.monthcalendar(year, month)\n    print("Mo Tu We Th Fr Sa Su")\n    for week in month_calendar:\n        week_str = ""\n        for day in week:\n            if day == 0:\n                week_str += "   "\n            else:\n                if day == now.day:\n                    week_str += f"*{day:2} "\n                else:\n                    week_str += f" {day:2} "\n        print(week_str.strip())\n\ndef add_event(event):\n    events.append(event)\n\ndef list_events():\n    for event in events:\n        print(f"{event.date} {event.time} - {event.title}: {event.description} (remind {event.reminder_minutes} min before)")\n\nif __name__ == "__main__":\n    render_month()\n    # Example usage: add_event(Event('2023-10-15', '10:00', 'Meeting', 'Discuss project', 30))\n    # list_events()\n