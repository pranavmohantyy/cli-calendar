import calendar\nimport datetime\nimport json\nimport os\nfrom dataclasses import dataclass\n\n@dataclass\nclass Event:\n    date: str\n    time: str\n    title: str\n    description: str\n    reminder_minutes: int\n    recurrence: str = None\n\nevents = []\n\ndef load_events():\n    if os.path.exists("~/.calendar_events.json"):\n        with open(os.path.expanduser("~/.calendar_events.json"), 'r') as f:\n            return json.load(f)\n    return []\n\ndef save_events():\n    with open(os.path.expanduser("~/.calendar_events.json"), 'w') as f:\n        json.dump([event.__dict__ for event in events], f)\n\ndef due_soon(event):\n    event_time = datetime.datetime.strptime(event.date + ' ' + event.time, '%Y-%m-%d %H:%M')\n    return event_time - datetime.datetime.now() < datetime.timedelta(hours=24)\n\ndef display_events():\n    for event in events:\n        if due_soon(event):\n            print(f'\033[93m{event.title} - Due Soon!\033[0m')  # Warning color\n        else:\n            print(event.title)\n\nevents = load_events()\n