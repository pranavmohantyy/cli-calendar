import calendar\nimport datetime\nimport json\nimport os\nfrom dataclasses import dataclass\n\n@dataclass\nclass Event:\n    date: str\n    time: str\n    title: str\n    description: str\n    reminder_minutes: int\n    recurrence: str = None\n\nevents = []\n\ndef load_events():\n    if os.path.exists("~/.calendar_events.json"):\n        with open(os.path.expanduser("~/.calendar_events.json"), 'r') as f:\n            return json.load(f)\n    return []\n\n\ndef save_events():\n    with open(os.path.expanduser("~/.calendar_events.json"), 'w') as f:\n        json.dump([event.__dict__ for event in events], f)\n\n\ndef agenda_view():\n    upcoming_events = sorted(events, key=lambda e: (e['date'], e['time']))\n    return upcoming_events\n\n# Load events on startup\nevents = load_events()