import calendar\nimport datetime\nimport json\nimport os\nfrom dataclasses import dataclass\n\n@dataclass\nclass Event:\n    date: str\n    time: str\n    title: str\n    description: str\n    reminder_minutes: int\n    recurrence: str = None\n\nevents = []\n\ndef load_events():\n    if os.path.exists(os.path.expanduser("~/.calendar_events.json")):\n        with open(os.path.expanduser("~/.calendar_events.json"), 'r') as f:\n            return json.load(f)\n    return []\n\ndef save_events():\n    with open(os.path.expanduser("~/.calendar_events.json"), 'w') as f:\n        json.dump([event.__dict__ for event in events], f)\n\ndef edit_event(event_index):\n    if 0 <= event_index < len(events):\n        event = events[event_index]\n        print(f"Editing event: {event.title}\")\n        title = input("New title (leave blank for no change): ")\n        if title:\n            event.title = title\n        description = input("New description (leave blank for no change): ")\n        if description:\n            event.description = description\n        time = input("New time (leave blank for no change): ")\n        if time:\n            event.time = time\n        reminder = input("New reminder minutes (leave blank for no change): ")\n        if reminder:\n            event.reminder_minutes = int(reminder)\n        print("Event updated!\")\n    else:\n        print("Invalid event index.")\n\ndef delete_event(event_index):\n    if 0 <= event_index < len(events):\n        confirm = input("Are you sure you want to delete this event? (y/n): ").lower()\n        if confirm == 'y':\n            events.pop(event_index)\n            print("Event deleted!\")\n        else:\n            print("Delete cancelled.")\n    else:\n        print("Invalid event index.")\n\n# Example usage (for testing):\n# events.append(Event(date='2023-10-01', time='10:00', title='Meeting', description='Team meeting', reminder_minutes=30))\n# edit_event(0)\n# delete_event(0)\n