import calendar\nimport datetime\nimport json\nimport os\nfrom dataclasses import dataclass\n\n@dataclass\nclass Event:\n    date: str\n    time: str\n    title: str\n    description: str\n    reminder_minutes: int\n    recurrence: str = None\n\nevents = []\n\ndef load_events():\n    if os.path.exists(os.path.expanduser("~/.calendar_events.json")):\n        with open(os.path.expanduser("~/.calendar_events.json"), 'r') as f:\n            return json.load(f)\n    return []\n\ndef save_events():\n    with open(os.path.expanduser("~/.calendar_events.json"), 'w') as f:\n        json.dump([event.__dict__ for event in events], f)\n\ndef export_to_ics():\n    ics_content = "BEGIN:VCALENDAR\nVERSION:2.0\n"\n    for event in events:\n        ics_content += f"BEGIN:VEVENT\nSUMMARY:{event.title}\nDESCRIPTION:{event.description}\nDTSTART:{event.date}T{event.time}\nEND:VEVENT\n"\n    ics_content += "END:VCALENDAR"\n    with open(os.path.expanduser("~/calendar_events.ics"), 'w') as f:\n        f.write(ics_content)\n\n# Add other functions to display events, reminders, etc.